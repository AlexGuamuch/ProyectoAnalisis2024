@model IEnumerable<SistemaSeguridad.Models.EstadoCivil>

@{
    ViewData["Title"] = "Listado de Estados Civiles";
}

<!-- Contenedor principal -->
<div class="custom-background p-4">

    <!-- Títulos -->
    <h1 class="mb-4 text-black">@ViewData["Title"]</h1>

    <!-- Tabla -->
    <div class="table-responsive">
        <table class="table table-striped table-bordered bg-white">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">Nombre</th>
                    <th scope="col">Fecha Creación</th>
                    <th scope="col">Usuario Creación</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var estado in Model)
                {
                    <tr>
                        <td class="text-truncate" style="max-width: 200px;">@estado.Nombre</td>
                        <td class="text-truncate" style="max-width: 200px;">
                            @estado.FechaCreacion.ToString("dd/MM/yyyy")
                        </td>
                        <td class="text-truncate" style="max-width: 200px;">@estado.UsuarioCreacion</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Estilos CSS -->
<style>
    .custom-background {
        background-color: #ffffff;
        border-radius: 10px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .bg-white {
        background-color: #ffffff;
    }

    .text-black {
        color: black;
    }

    .thead-dark {
        background-color: #ffffff;
        color: black;
    }

    .table th {
        text-align: center;
    }

    .text-truncate {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .table td {
        font-size: 0.875rem;
        padding: 0.5rem;
    }
</style>
